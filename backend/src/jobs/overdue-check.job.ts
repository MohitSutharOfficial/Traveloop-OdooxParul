// Overdue check job disabled (Prisma removed)
// Re-enable this when database is configured

export const startOverdueCheckJob = () => {
  console.log('ℹ️  Overdue check job disabled (database not configured)');
  // Job disabled - requires database
  // Uncomment and configure when Prisma is reinstalled
};
