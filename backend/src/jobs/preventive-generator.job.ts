// Preventive maintenance generator job disabled (Prisma removed)
// Re-enable this when database is configured

export const startPreventiveGeneratorJob = () => {
  console.log('ℹ️  Preventive generator job disabled (database not configured)');
  // Job disabled - requires database
  // Uncomment and configure when Prisma is reinstalled
};
